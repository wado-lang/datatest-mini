[package]
name = "datatest-mini"
version = "0.1.0"
edition = "2024"
description = "Minimal proc_macro for generating test functions from fixture files"
license = "MIT"
publish = false

[lib]
proc-macro = true

[dependencies]
regex = "1"
